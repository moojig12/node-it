-- Optional: additional initialization SQL
-- The database "node_notes" is created automatically by the POSTGRES_DB env var.
-- Add any seed data or extensions here if needed.

-- Example: Enable UUID extension (used by Prisma's @default(uuid()))
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
