export default function Page() {
  return (
    <main className="hero">
      <h1>Node It</h1>
      <p className="muted">
        Next.js scaffold — Phase 2.5 migration in progress.
      </p>
      <ul className="checklist">
        <li>App Router</li>
        <li>Route handlers next</li>
        <li>Vite frontend stays running on port 3000 until parity</li>
      </ul>
    </main>
  );
}
