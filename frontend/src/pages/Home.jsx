import CanvasView from '../components/canvas/CanvasView.jsx';

// The Home route is the canvas now — categories laid out as dots, with
// the persistent rail on the left providing list-style access.
function Home() {
  return <CanvasView />;
}

export default Home;
