# Local development — used by Prisma CLI (migrate, generate, studio, etc.)
# For Docker, DATABASE_URL is injected via docker-compose.yml environment block.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/node_notes?schema=public"
